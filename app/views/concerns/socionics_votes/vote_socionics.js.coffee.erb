$link = $("a#vote-<%= @vote_type %>");

if (<%- current_user.voted_on? @votable %>) {
  $link.html("<%= j image_tag('upvoted.png') %>");
  $link.attr("class", true);
} else {
  $link.html("<%= j image_tag('upvote.png') %>");
  $link.attr("class", false);
}


