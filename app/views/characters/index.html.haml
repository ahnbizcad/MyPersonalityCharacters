%h1 Listing characters

%table
  %tr
    %th
    %th
    %th

  - @characters.each do |char|
    %tr
      
      %td= link_to char.name, char
      
      %td= link_to 'Edit', edit_character_path(char)
      %td= link_to 'Destroy', char, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Character', new_character_path
